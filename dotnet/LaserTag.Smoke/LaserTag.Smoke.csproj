<Project Sdk="Microsoft.NET.Sdk">

  <!-- Throwaway smoke/demo harness for exercising LaserTag.Client against a live
       device. NOT the real CLI (that is a later, separate plan). -->
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\LaserTag.Client\LaserTag.Client.csproj" />
  </ItemGroup>

</Project>
